apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "habagou.fullname" . }}-env
  labels:
    {{- include "habagou.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
