Habagou has been deployed.

{{- if .Values.ingress.enabled }}
The service is exposed through the configured ingress:
  https://{{ .Values.ingress.hostname }}

Production smoke check:
  just smoke BASE_URL=https://{{ .Values.ingress.hostname }}
{{- else }}
Ingress is disabled.
{{- end }}
