apiVersion: v1
kind: Service
metadata:
  name: {{ include "habagou.fullname" . }}
  labels:
    {{- include "habagou.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "habagou.selectorLabels" . | nindent 4 }}
